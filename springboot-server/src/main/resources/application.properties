spring.application.name=springboot-server
server.port=8080

# Let unmatched routes raise NoHandlerFoundException so GlobalExceptionHandler can format
# a 404 with the same JSON error shape as the rest of the API, instead of Spring's default page.
spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false
